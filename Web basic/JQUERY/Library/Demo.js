$(document).ready(function() {
    $("button").click(function() {            //event method
        $('p').hide();                         //effect method
    });
});

//when document becomes ready then only annonemous function gets executed
//when user clicks on the button then only annonemous function gets executed